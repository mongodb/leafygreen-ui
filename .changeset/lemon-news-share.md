---
'@leafygreen-ui/menu': major
---

Removes the closing of `Menu` on `MenuItem` click. This behavior was causing issues with clickable items inside of `MenuItem`. To control when `Menu` closes, use `open` and `setOpen`. [LG-4130](https://jira.mongodb.org/browse/LG-4130)
