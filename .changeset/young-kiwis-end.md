---
'@lg-tools/create': minor
---

Updated default exported component interface to extend LgIdProps, as well as the readme template to dynamically use the scope instead of the previously hard-coded value.
