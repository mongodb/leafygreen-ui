---
'@leafygreen-ui/pagination': minor
---

Exports `PaginationRangeView`, `PaginationItemsPerPage`, and `PaginationCurrentPageControls` components, allowing consumers to import only the specific parts of the Pagination UI they need
