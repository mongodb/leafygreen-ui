---
'@leafygreen-ui/mongo-nav': major
---

Exposes imperative API to refetch data in MongoNav: `mongoNavRef.current.reload()`. This is a major version bump, because React recommends that when a component is newly wrapped in `forwardRef` it be released as a breaking change.
