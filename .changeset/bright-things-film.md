---
'@leafygreen-ui/tabs': major
---

[LG-4696](https://jira.mongodb.org/browse/LG-4696): Rename `selected` prop to `value` and `setSelected` prop to `onValueChange` and update `onValueChange` type signature to `(number | string) => void;`
