---
'@leafygreen-ui/menu': patch
---

Adds `id` prop and adds `aria-expanded` and `aria-haspopup` internally. Removes `MenuItem.displayName` and passes the displayName as an argument to `InferredPolymorphic` instead.
