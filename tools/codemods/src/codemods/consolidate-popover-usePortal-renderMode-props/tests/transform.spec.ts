import { transformTest } from '../../../utils/tests/transformTest';

transformTest(__dirname, {
  fixture: 'consolidate-popover-usePortal-renderMode-props',
  transform: 'consolidate-popover-usePortal-renderMode-props',
});
