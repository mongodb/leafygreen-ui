import { css } from '@emotion/css';
import { Body } from '@leafygreen-ui/typography';
import GuidelineImage from 'components/GuidelineImage';
import { CDN } from 'utils/routes';

<Body
  className={css`
    margin-top: 24px;
  `}
>
  Buttons are a type of call to action (CTA) the user can click or press.
  Buttons can also be triggers for opening menu dropdowns. In this scenario,
  consider using a caret as an extra affordance that there are other actions
  nested.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/button/button-guidelines-1@3x.png`}
  className={css`
    width: auto;
    height: 174px;
  `}
/>

<Body>
  Green, or primary buttons are reserved for constructive/positive actions, like
  building a new cluster.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/button/button-guidelines-2@3x.png`}
  className={css`
    width: auto;
    height: 47px;
  `}
/>

<Body>
  On screens where there are many green CTAs you may opt to use the outlined
  green CTA to differentiate emphasis.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/button/button-guidelines-3@3x.png`}
  className={css`
    width: auto;
    height: 335px;
  `}
/>

<Body>
  Red buttons should be used to show destructive CTAs, like deleting a backup.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/button/button-guidelines-4@3x.png`}
  className={css`
    width: auto;
    height: 51px;
  `}
/>

<Body>
  A secondary destructive CTA is used when there are several destructive CTAs in
  proximity to each other on a page. Having all destructive CTAs filled can seem
  overpowering to the user.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/button/button-guidelines-5@3x.png`}
  className={css`
    width: auto;
    height: 142px;
  `}
/>

<Body>
  The default, or neutral button is gray and should be used for actions like
  close or cancel.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/button/button-guidelines-6@3x.png`}
  className={css`
    width: auto;
    height: 51px;
  `}
/>

<Body>
  Buttons come in various sizes. For some, we also support 16px font size
  versions for platforms that need it.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/button/button-guidelines-7@3x.png`}
  className={css`
    width: auto;
    height: 381px;
  `}
/>

<Body>
  Keep button content sentence case. Only capitalize the first letter of the CTA
  and the first letter of a proper noun, like Atlas, Data Lake, Ops Manager,
  etc.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/button/button-guidelines-8@3x.png`}
  className={css`
    width: auto;
    height: 88px;
  `}
/>

<Body>
  Buttons are available for both light and dark mode.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/button/button-guidelines-9@3x.png`}
  className={css`
    width: auto;
    height: 205px;
  `}
/>
