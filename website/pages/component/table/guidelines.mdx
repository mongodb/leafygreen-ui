import { css } from '@emotion/css';
import { Body, Subtitle, Link } from '@leafygreen-ui/typography';
import GuidelineImage from 'components/GuidelineImage';
import { CDN } from 'utils/routes';
import { mq } from 'utils/mediaQuery';

<Body
  className={css`
    margin-top: 24px;
  `}
>
  Info tables display static data, and editable tables, are editable (like a
  spreadsheet).
</Body>

<Body
  className={css`
    margin-top: 24px;
  `}
>
  The header of both these types of tables is the same. You can use the sort
  icons to sort columns in descending or ascending order. By default these
  columns are unsorted.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-1@3x.png`}
  className={css`
    width: auto;
    height: 41px;
  `}
/>

<Body>
  A menu trigger can be added to each column header, for example to allow the
  user to delete a column.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-2@3x.png`}
  className={css`
    width: auto;
    height: 87px;
  `}
/>

<Body>
  When your info table spans more than 10 rows you should alternate the
  background color of each row, so that your table is more scannable.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-3@3x.png`}
  className={css`
    height: 520px;
    ${mq({
      width: ['auto', 'auto', 'auto', '946px'],
    })}
  `}
/>

<Body>
  Do not use the alternating background pattern when there are fewer than 10
  line items in your table. Otherwise it may look like one of the rows is
  selected or disabled.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-4@3x.png`}
  className={css`
    height: 179px;
    ${mq({
      width: ['auto', 'auto', 'auto', '1053px'],
    })}
  `}
/>

<Subtitle>Content alignment</Subtitle>

<Body>
  Text should be left aligned. Numbers expressing quantity should be right
  aligned. Dates can be either left or right aligned.{' '}
</Body>

<Link href="https://practicaltypography.com/grids-of-numbers.html">
  Learn more about content alignment.
</Link>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-5@3x.png`}
  className={css`
    height: 155px;
    ${mq({
      width: ['auto', 'auto', 'auto', '991px'],
    })}
  `}
/>

<Subtitle>Disabled Rows</Subtitle>
<Body>
  Disabled rows have a darker background and lighter copy and show the disabled
  cursor on hover. Do not use these cells alongside alternate colored rows as
  this will make it difficult to distinguish which row is disabled and which is
  not.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-6@3x.png`}
  className={css`
    height: 265px;
    ${mq({
      width: ['auto', 'auto', 'auto', '1098px'],
    })}
  `}
/>

<Subtitle>Nesting</Subtitle>
<Body>
  Avoid nesting tables within other tables or within other elements. These are
  both challenging to read and build.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-7@3x.png`}
  className={css`
    height: 483px;
    ${mq({
      width: ['auto', 'auto', 'auto', '1126px'],
    })}
  `}
/>

<Subtitle>Expandable Rows</Subtitle>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-8@3x.png`}
  className={css`
    height: 204px;
    ${mq({
      width: ['auto', '503px'],
    })}
  `}
/>

<Subtitle>Row Selection</Subtitle>
<Body>
  An entire row can also be selected editing, or the entire table can be bulk
  selected.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-9@3x.png`}
  className={css`
    height: 231px;
    ${mq({
      width: ['auto', 'auto', 'auto', '811px'],
    })}
  `}
/>

<Body>
  If your info table has clickable rows, use the "Hover Shadow" as a visual
  affordance.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-10@3x.png`}
  className={css`
    height: 204px;
    ${mq({
      width: ['auto', 'auto', 'auto', '1000px'],
    })}
  `}
/>

<Body>
  In some use-cases your table might become too big to be viewed all at once on
  screen. In these scenarios, you can use overflow shadows to indicate
  scrollable areas within your table.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/infoTable/infoTable-guidelines-11@3x.png`}
  className={css`
    height: 254px;
    ${mq({
      width: ['auto', 'auto', 'auto', '1000px'],
    })}
  `}
/>
