import { css } from 'emotion';
import Card from '@leafygreen-ui/card';
import { Body, Link } from '@leafygreen-ui/typography';
import { Table, HeaderRow, TableHeader, Row, Cell } from '@leafygreen-ui/table';
import { uiColors } from '@leafygreen-ui/palette';
import GuidelineImage from 'components/GuidelineImage';
import { CDN } from 'utils/routes';
import { mq } from 'utils/mediaQuery';

<Body
  className={css`
    margin-top: 24px;
  `}
>
  Toasts are a form of notification. They should be used fairly sparingly, since
  they animate and can become overwhelming to users if used to confirm every
  action.
</Body>

<Body
  className={css`
    margin-top: 24px;
    margin-bottom: 36px;
  `}
>
  Below is a table to help you choose when to pick which pattern for
  notifications.
</Body>

<Card
  className={css`
    margin-bottom: 36px;
  `}
>
  <Table
    data={[
      {
        component: 'Toast',
        priority: 'Low priority',
        userAction: 'Optional: Toasts disappear automatically',
      },
      {
        component: 'Banner',
        priority: 'Medium priority',
        userAction:
          'Optional: Banners remain until dismissed by the user, or if the state, which caused the banner is resolved',
      },
      {
        component: 'Modal',
        priority: 'Highest priority',
        userAction:
          'Required: Modals block usage until the user takes a modal action or exits the modal (if available)',
      },
    ]}
    columns={[
      <TableHeader
        className={css`
          min-width: 120px;
        `}
        key="component"
        label="Component"
      />,
      <TableHeader key="priority" label="Priority" />,
      <TableHeader key="userAction" label="User Action" />,
    ]}
  >
    {({ datum }) => (
      <tr
        key={datum.component}
        className={css`
          border-bottom: 1px solid ${uiColors.gray.light2};
        `}
      >
        <Cell
          className={css`
            padding: 16px 0 16px 10px;
          `}
        >
          {datum.component}
        </Cell>
        <Cell
          className={css`
            padding: 16px 0 16px 10px;
          `}
        >
          {datum.priority}
        </Cell>
        <Cell
          className={css`
            padding: 16px 0 16px 10px;
          `}
        >
          {datum.userAction}
        </Cell>
      </tr>
    )}
  </Table>
</Card>

<Body>Toasts are always placed at the bottom left of a screen.</Body>

<Body
  className={css`
    margin-top: 24px;
  `}
>
  Toasts can be used to confirm an action. For example, here’s a confirmation
  toast that shows up after an email has been sent.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/toast/toast-guidelines-1@3x.png`}
  className={css`
    ${mq({
      width: ['auto', '448px'],
    })}
    height: 90px;
  `}
/>

<Body>
  They can be used to indicate something important may need to be addressed.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/toast/toast-guidelines-2@3x.png`}
  className={css`
    ${mq({
      width: ['auto', '432px'],
    })}
    height: 99px;
  `}
/>

<Body>
  They can be used to show general information that may be helpful to the user.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/toast/toast-guidelines-3@3x.png`}
  className={css`
    ${mq({
      width: ['auto', '432px'],
    })}
    height: 99px;
  `}
/>

<Body>
  Toasts can either be dismissable (via the top "X"), or can disappear
  automatically after a set amount of time — to figure out this time, factor for
  a reading speed of 150 words per minute, plus allow for another second or so
  to allow for enough time for the user to read the toast content).
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/toast/toast-guidelines-4@3x.png`}
  className={css`
    ${mq({
      width: ['auto', 'auto', 'auto', '898px'],
    })}
    height: 90px;
  `}
/>

<Body>
  Do not use toasts for components that have notifications integrated into their
  designs like{' '}
  <Link href="https://www.mongodb.design/component/copyable/example/">
    copyable
  </Link>
  .
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/toast/toast-guidelines-5@3x.png`}
  className={css`
    ${mq({
      width: ['auto', 'auto', 'auto', '825px'],
    })}
    height: 159px;
  `}
/>

<Body>
  Toasts can also be used to show status of longer ongoing processes (lasting
  more than 4-5 seconds). When doing so, try to add an estimate of the time the
  process will take to complete, or where in the process the user is.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/toast/toast-guidelines-6@3x.png`}
  className={css`
    ${mq({
      width: ['auto', '431px'],
    })}
    height: 105px;
  `}
/>

<Body>
  Do not nest actions within toasts. Our progress toast allows for an action to
  be contained within it, such as for viewing the current progress of a cluster
  rebuild or pausing the current process.
</Body>
