import { css } from '@emotion/css';
import { Body, Link } from '@leafygreen-ui/typography';
import GuidelineImage from 'components/GuidelineImage';
import { CDN } from 'utils/routes';

<Body
  className={css`
    margin-top: 24px;
  `}
>
  Checkboxes should be used whenever a user has an option they’d like to opt in
  or out of. Unlike toggles, checkboxes are used for actions, or features, that
  don’t immediately turn on or off. Checkboxes are usually found in forms as
  opposed to config pages.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/checkbox/checkbox-guidelines-1@3x.png`}
  className={css`
    width: auto;
    height: 202px;
  `}
/>

<Body>
  When a toggle is disabled. Try to provide context somewhere as to why it’s
  disabled. Consider using a tooltip in these scenarios.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/checkbox/checkbox-guidelines-2@3x.png`}
  className={css`
    width: auto;
    height: 120px;
  `}
/>

<Body>Sometimes it’s possible to have nested checkboxes.</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/checkbox/checkbox-guidelines-3@3x.png`}
  className={css`
    width: auto;
    height: 155px;
  `}
/>

<Body>
  Use an indeterminate checkbox to indicate that there are both selected and
  unselected checkboxes nested under the indeterminate checkbox, for example in
  tables.
  <ul>
    <li>
      Clicking on an indeterminate checkbox makes all checkboxes selected.
    </li>
    <li>Clicking on a selected checkbox makes all checkboxes deselected.</li>
    <li>Clicking on a deselected checkbox makes all checkboxes selected.</li>
  </ul>
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/checkbox/checkbox-guidelines-4@3x.png`}
  className={css`
    width: auto;
    height: 321px;
  `}
/>

<Body>Checkboxes are available for both light and dark mode.</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/checkbox/checkbox-guidelines-5@3x.png`}
  className={css`
    width: auto;
    height: 194px;
  `}
/>
