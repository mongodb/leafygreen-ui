import { css } from '@emotion/css';
import { Body, Link } from '@leafygreen-ui/typography';
import Callout from '@leafygreen-ui/callout';
import GuidelineImage from 'components/GuidelineImage';
import { CDN } from 'utils/routes';
import { mq } from 'utils/mediaQuery';

Code components are used to show snippets of code.

There are 4 styles of the code component. We recommend you use the sidebar
style for most use-cases.

<GuidelineImage
  className={css`
    width: auto;
    height: 492px;
  `}
  imageURL={`${CDN}/images/new-guidelines/code/code-guidelines-1@3x.png`}
/>

<Callout
  variant="note"
  className={css`
    margin-bottom: 24px;
  `}
>
In order to view syntax highlighting in Figma you will need to 
[install the Figma Code Highlighter plugin](https://www.figma.com/community/plugin/764836637878472379/Figma-Code-Highlighter).
This will emulate similar highlighting found in production. Use the atomOneLight
highlighter theme in light mode; use Agate for dark mode.

</Callout>

Only use the Source Code Pro font within the code component.

<GuidelineImage
  className={css`
    height: 200px;
    ${mq({
      width: ['auto', 'auto', 'auto', '756px'],
    })}
  `}
  imageURL={`${CDN}/images/new-guidelines/code/code-guidelines-2@3x.png`}
/>

Try to keep the max-width of the code component at 775px wide when used 16px
font areas. For 14px base font areas, try to keep the code component max-width
to 700px. If the content in the code snippet is very large this recommendation
can ignored.

<GuidelineImage
  className={css`
    height: 200px;
    ${mq({
      width: ['auto', 'auto', 'auto', '1050px'],
    })}
  `}
  imageURL={`${CDN}/images/new-guidelines/code/code-guidelines-3@3x.png`}
/>

Whenever possible, align the code component and other components to the 4px
baseline grid.

<GuidelineImage
  className={css`
    height: 441px;
    ${mq({
      width: ['auto', '468px'],
    })}
  `}
  imageURL={`${CDN}/images/new-guidelines/code/code-guidelines-4@3x.png`}
/>

Use an overflow shadow as to show that there’s more content that can be seen
when scrolling.

<GuidelineImage
  className={css`
    height: 296px;
    ${mq({
      width: ['auto', 'auto', 'auto', '756px'],
    })}
  `}
  imageURL={`${CDN}/images/new-guidelines/code/code-guidelines-5@3x.png`}
/>
