import { css } from '@emotion/css';
import { H2, Body, Link } from '@leafygreen-ui/typography';
import GuidelineImage from 'components/GuidelineImage';
import { CDN } from 'utils/routes';

<H2>Grid</H2>

<Body
  className={css`
    margin-top: 24px;
  `}
>
  Grids help you design your screens. In LeafyGreen Figma you have 7 grids to
  chose from.
</Body>
<Body
  className={css`
    margin-top: 24px;
  `}
>
  Depending on the size of the grid, you may have more or less columns. The size
  of the gutter (space between columns) and margin (space on each horizontal
  edge of the grid) may change.
</Body>
<Body
  className={css`
    margin-top: 24px;
  `}
>
  In LeafyGreen Figma we’ve also accounted space for our collapsed or expanded
  sideNav component.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/grid/grid-guidelines-1@3x.png`}
  className={css`
    width: auto;
    height: 457px;
  `}
/>

<Body>
  Grids are useful for defining responsive patterns. Depending on how many
  collumns your UI elements stretch you can define how they should behave on
  different screen and browser sizes.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/grid/grid-guidelines-2@3x.png`}
  className={css`
    width: auto;
    height: 215px;
  `}
/>

<Body>
  Consider implementing a max-width when designing. On some pages on
  mongoDB.design you’ll notice that some content only extends up to a certain
  point on the largest of screens.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/grid/grid-guidelines-3@3x.png`}
  className={css`
    width: auto;
    height: 421px;
  `}
/>

<Body>
  Our 4px baseline grid is useful for aligning type and other UI elements.{' '}
  <Link href="https://www.mongodb.design/component/typography/example/">
    Learn more about type system.
  </Link>
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/grid/grid-guidelines-4@3x.png`}
  className={css`
    width: auto;
    height: 415px;
  `}
/>

<Body>
  In addition to our grids you can use our spacer blocks to help you space UI
  elements from eaach other.
</Body>

<GuidelineImage
  imageURL={`${CDN}/images/new-guidelines/grid/grid-guidelines-5@3x.png`}
  className={css`
    width: auto;
    height: 113px;
  `}
/>
