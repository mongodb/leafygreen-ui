import PageLoader, { type PageLoaderProps } from './PageLoader';
import Spinner, { type SpinnerProps } from './Spinner';
export {
  PageLoader,
  type PageLoaderProps,
  /**
   * @deprecated - This package installs and bundles Lottie player, a heavy run-time animation library.
   * For simple loading spinners, prefer using the `@leafygreen-ui/loading-spinner` package
   */
  Spinner,
  type SpinnerProps,
};
