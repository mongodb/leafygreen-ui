
import React from 'react';
import { render, cleanup } from '@testing-library/react';
import SideNav from '.';

afterAll(cleanup);

describe('packages/side-nav', () => {
  test('condition', () => {

  })
})
