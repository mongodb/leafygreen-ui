describe('packages/toast/controlled', () => {
  test.todo('does not render when `open` is false');
  test.todo('renders when `open` is true');
  test.todo('is closed when `open` is set back to false');
  test.todo('does not close until manually closed');

  test.todo('`onClose` method is called when the close button is clicked');
  test.todo('`onClose` method is called after timeout');
});
