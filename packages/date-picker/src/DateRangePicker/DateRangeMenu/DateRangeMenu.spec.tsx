import React from 'react';
import { render } from '@testing-library/react';

import { DateRangeMenu } from '.';

describe('packages/date-range-menu', () => {
  test.todo(
    'Quick Select buttons fire a change handler with the correct payload',
  );
});
