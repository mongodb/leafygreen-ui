import React from 'react';
import { render, cleanup } from '@testing-library/react';
import Select from '.';

afterAll(cleanup);

describe('packages/select', () => {
  test('condition', () => {});
});
