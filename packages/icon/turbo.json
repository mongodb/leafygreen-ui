{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "prebuild": {
      "dependsOn": ["^prebuild"],
      "inputs": ["src/glyphs/*.svg"],
      "outputs": ["src/generated/*.tsx"]
    }
  }
}
