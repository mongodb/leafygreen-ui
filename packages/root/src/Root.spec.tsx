
import React from 'react';
import { render, cleanup } from '@testing-library/react';
import Root from '.';

afterAll(cleanup);

describe('packages/root', () => {
  test('condition', () => {

  })
})
