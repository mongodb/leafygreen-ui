
import React from 'react';
import { render, cleanup } from '@testing-library/react';
import MongoCombobox from '.';

afterAll(cleanup);

describe('packages/mongo-combobox', () => {
  test('condition', () => {

  })
})
