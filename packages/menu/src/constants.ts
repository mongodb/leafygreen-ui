const LGID_ROOT = 'lg-menu';

export const LGIDs = {
  root: LGID_ROOT,
  item: LGID_ROOT + '-menu_item',
  submenu: LGID_ROOT + '-submenu',
  submenuToggle: LGID_ROOT + '-submenu_toggle',
} as const;
