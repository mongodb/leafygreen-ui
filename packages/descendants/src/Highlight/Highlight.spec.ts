describe('packages/descendants/highlight', () => {
  describe('useHighlight', () => {
    test.todo('returns the current highlight object, and setter function(s)');
    test.todo('calling the setter updates the highlight object');
    test.todo(
      '`onInit` option is called once when descendants are initialized',
    );
    test.todo('`onChange` option is called when the highlight object changes');
    test.todo(
      '`filter` option prevents relevant descendants from being highlighted',
    );
  });

  describe('useHighlightContext', () => {
    test.todo('returns the current highlight object');
  });
});
