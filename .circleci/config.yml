version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15
    steps:
      - checkout
      - run: npm ci
      - run: npx lerna bootstrap
      - run: npm run build
      - run:
          name: Tests
          command: npm run test -- --no-cache --ci --runInBand
