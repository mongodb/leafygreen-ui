version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - corepack prepare pnpm --activate
        - pnpm setup
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm run build
