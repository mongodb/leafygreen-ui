import { css } from '@leafygreen-ui/emotion';
import { spacing } from '@leafygreen-ui/tokens';

export const seriesListStyles = css`
  all: unset;
  padding: 0 ${spacing[150]}px ${spacing[150]}px;
  display: grid;
  gap: ${spacing[100]}px;
`;
