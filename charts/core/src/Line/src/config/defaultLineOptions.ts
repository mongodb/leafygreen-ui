export const defaultSeriesOption: {
  type: 'line';
  showSymbol: boolean;
  symbol: string;
  clip: boolean;
  emphasis: {
    disabled: boolean;
  };
} = {
  type: 'line',
  showSymbol: false,
  symbol: 'circle',
  clip: false,
  emphasis: {
    disabled: true,
  },
};
